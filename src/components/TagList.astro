---
interface TagObject {
	name: string;
	slug: string;
}

interface Props {
	tags: (string | TagObject)[];
}

const { tags } = Astro.props;

function getTagName(tag: string | TagObject): string {
	return typeof tag === 'string' ? tag : tag.name;
}

function getTagSlug(tag: string | TagObject): string {
	return typeof tag === 'string' ? tag : tag.slug;
}
---

{
	tags.length > 0 && (
		<ul class="tag-list" role="list">
			{tags.map((tag) => (
				<li>
					<a href={`/blog/tags/${getTagSlug(tag)}/`} class="tag">
						{getTagName(tag)}
					</a>
				</li>
			))}
		</ul>
	)
}

<style>
	.tag-list {
		list-style: none;
		margin: 0;
		padding: 0;
		display: flex;
		flex-wrap: wrap;
		gap: var(--sp-2);
	}
	.tag {
		/*
		 * position + z-index lifts tags above the card-link::after overlay
		 * so they remain clickable even when inside a stretched-link card.
		 */
		position: relative;
		z-index: 1;
		display: inline-block;
		font-family: var(--font-mono);
		font-size: var(--fs-xs);
		font-weight: 500;
		padding: 2px var(--sp-3);
		color: var(--color-ink-muted);
		background-color: var(--color-surface);
		border: 1px solid var(--color-border);
		border-radius: var(--radius-pill);
		text-decoration: none;
		transition: background-color var(--ease), color var(--ease);
	}
	.tag:hover {
		background-color: var(--color-ink);
		color: var(--color-bg);
		border-color: var(--color-ink);
	}
</style>
