---
import { runSDK, SDKCoreJs } from 'studiocms:sdk';
import BaseHead from '../../components/BaseHead.astro';
import Footer from '../../components/Footer.astro';
import Header from '../../components/Header.astro';
import ProjectCard from '../../components/ProjectCard.astro';
import StatusBadge from '../../components/StatusBadge.astro';
import { SITE_DESCRIPTION, SITE_TITLE } from '../../consts';

const projects = await runSDK(SDKCoreJs.GET.folderPages('projects'));

const [featured, ...rest] = projects;
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead title={`Projects — ${SITE_TITLE}`} description={SITE_DESCRIPTION} />
	</head>
	<body>
		<Header />
		<main>
			<header class="page-header">
				<h1>Projects</h1>
				<p class="page-subtitle">Things built, automated, and occasionally finished.</p>
				<div class="projects-stats">
					<span><span class="stat-num">{projects.length}</span> total</span>
				</div>
			</header>

			{featured && (
			<div class="featured-project">
				{featured.heroImage && (
					<div class="featured-image">
						<img width="720" height="405" src={featured.heroImage} alt="" loading="lazy" />
					</div>
				)}
				<div class="featured-body">
					<h2 class="featured-title">
						<a href={`/projects/${featured.slug}/`} class="card-link">
							{featured.title}
						</a>
					</h2>
					<p class="featured-description">{featured.description}</p>
				</div>
			</div>
			)}

			<div class="grid-header">
				<span class="grid-label">All projects</span>
				<div class="grid-rule"></div>
			</div>

			<ul class="project-grid" role="list">
				{rest.map((project, i) => (
					<ProjectCard
						project={project}
						style={`animation-delay:${Math.min(0.1 + i * 0.05, 0.35)}s`}
					/>
				))}
			</ul>
		</main>
		<Footer />
	</body>
</html>

<style>
	/* ── Page header ── */
	.page-header {
		margin-bottom: var(--sp-8);
	}
	.page-header h1 {
		font-size: var(--fs-5xl);
		margin-bottom: var(--sp-2);
	}
	.page-subtitle {
		margin: 0 0 var(--sp-4);
		font-family: var(--font-mono);
		font-size: var(--fs-sm);
		color: var(--color-ink-muted);
	}
	.projects-stats {
		display: flex;
		align-items: center;
		gap: var(--sp-3);
		font-family: var(--font-mono);
		font-size: var(--fs-xs);
		color: var(--color-ink-muted);
	}
	.stat-num {
		color: var(--color-ink);
		font-weight: 600;
	}
	.stat-sep {
		color: var(--color-border);
	}

	/* ── Featured project ── */
	.featured-project {
		position: relative;
		display: grid;
		grid-template-columns: 3fr 2fr;
		border: 1px solid var(--color-border);
		border-radius: var(--radius-lg);
		overflow: hidden;
		margin-bottom: var(--sp-12);
		background: var(--color-bg);
		transition: box-shadow var(--ease), border-color var(--ease);
	}
	.featured-project:hover {
		box-shadow: var(--shadow-md);
		border-color: var(--color-accent);
	}
	.featured-image {
		overflow: hidden;
	}
	.featured-image img {
		width: 100%;
		height: 100%;
		object-fit: cover;
		border-radius: 0;
		transition: transform var(--ease-slow);
	}
	.featured-project:hover .featured-image img {
		transform: scale(1.04);
	}
	.featured-body {
		padding: var(--sp-10);
		display: flex;
		flex-direction: column;
		gap: var(--sp-5);
		justify-content: center;
	}
	.featured-title {
		margin: 0;
		font-family: var(--font-body);
		font-size: var(--fs-3xl);
		line-height: 1.15;
	}
	.card-link {
		text-decoration: none;
		color: inherit;
		transition: color var(--ease);
	}
	.card-link::after {
		content: '';
		position: absolute;
		inset: 0;
	}
	.featured-project:hover .card-link {
		color: var(--color-accent);
	}
	.featured-description {
		margin: 0;
		font-size: var(--fs-md);
		color: var(--color-ink-muted);
		line-height: 1.65;
	}
	.featured-stat {
		display: flex;
		flex-direction: column;
		gap: 3px;
	}
	.stat-label {
		font-family: var(--font-mono);
		font-size: var(--fs-xs);
		color: var(--color-ink-muted);
		text-transform: uppercase;
		letter-spacing: 0.06em;
	}
	.stat-value {
		font-family: var(--font-display);
		font-size: var(--fs-xl);
		font-weight: 600;
		color: var(--color-ink);
	}

	/* ── Grid header ── */
	.grid-header {
		display: flex;
		align-items: center;
		gap: var(--sp-6);
		margin-bottom: var(--sp-6);
	}
	.grid-label {
		font-family: var(--font-mono);
		font-size: var(--fs-xs);
		color: var(--color-ink-muted);
		white-space: nowrap;
	}
	.grid-rule {
		flex: 1;
		height: 1px;
		background: var(--color-border);
	}

	/* ── Project grid ── */
	.project-grid {
		list-style: none;
		margin: 0;
		padding: 0;
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: var(--sp-8);
		animation: fadeUp 0.5s cubic-bezier(0.22, 1, 0.36, 1) both;
		animation-delay: 0.2s;
	}

	@keyframes fadeUp {
		from { opacity: 0; transform: translateY(8px); }
		to   { opacity: 1; transform: translateY(0); }
	}

	@media (prefers-reduced-motion: reduce) {
		.featured-project,
		.project-grid {
			animation: none;
		}
	}

	/* ── Responsive ── */
	@media (max-width: 900px) {
		.project-grid {
			grid-template-columns: repeat(2, 1fr);
		}
	}

	@media (max-width: 720px) {
		.page-header h1 {
			font-size: var(--fs-4xl);
		}
		.featured-project {
			grid-template-columns: 1fr;
		}
		.featured-image {
			aspect-ratio: 16 / 9;
		}
		.featured-body {
			padding: var(--sp-6);
		}
		.featured-title {
			font-size: var(--fs-2xl);
		}
		.project-grid {
			grid-template-columns: 1fr;
			gap: var(--sp-5);
		}
	}
</style>
